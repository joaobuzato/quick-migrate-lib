{
  "name": "quick-migrate-lib",
  "version": "0.0.10",
  "description": "Lib to auto migrate your project ",
  "main": "dist/src/index.js",
  "type": "module",
  "scripts": {
    "publish": "tsc --build && npm version patch --force && npm publish",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.5",
    "fs": "^0.0.1-security",
    "mysql": "^2.18.1"
  },
  "devDependencies": {
    "@types/mysql": "^2.15.25",
    "@types/node": "^20.11.20",
    "typescript": "^5.3.3"
  }
}
